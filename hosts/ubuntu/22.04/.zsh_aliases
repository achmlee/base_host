# ~/.zsh_aliases
# If using oh-my-zsh, make this $ZSH_CUSTOM/aliases.zsh

# General
alias cp='cp -iv'
alias grep='grep -in'
alias ls='ls -F'
alias mv='mv -iv'
alias rm='rm -i'

# System specific aliases
if [ -f ~/.zsh_aliases_local ]; then
  . ~/.zsh_aliases_local
fi

